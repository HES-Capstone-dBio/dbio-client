import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#443FB5",
    },
    secondary: {
      main: "#f9b934",
    },
  },
});

export default theme;
